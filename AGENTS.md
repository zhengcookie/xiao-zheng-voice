# AGENTS.md

## Agent skills

### Issue tracker

Issues live in GitHub Issues. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout — one `CONTEXT.md` at repo root + `docs/adr/`. See `docs/agents/domain.md`.

### Agent identity

Agent commits use a worktree-scoped git identity (never `git config` without `--worktree`). See `docs/agents/agent-identity.md`.
