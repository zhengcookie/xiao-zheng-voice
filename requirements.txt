# Xiao Zheng Voice - Dependencies
# Install with: pip install -r requirements.txt
#
# Versions are pinned exactly to keep pip's resolver fast and
# reproducible (a wide range makes pip backtrack for minutes).

# --- Web UI (required) ---
gradio==4.44.1
# gradio 4.44 依赖 huggingface_hub.HfFolder（1.0 已移除），锁到兼容的 0.28.1
huggingface_hub==0.28.1

# --- Audio I/O (required) ---
soundfile==0.12.1
pydub==0.25.1

# --- Numerics (required) ---
numpy==1.26.4
scipy==1.13.1

# --- Utilities ---
tqdm==4.66.5
pyyaml==6.0.2

# --- Optional: audio preprocessing (resampling, slicing, denoise) ---
# Heavy (pulls numba/llvmlite). Only needed for the preprocessing feature.
# librosa==0.10.2.post1

# --- Optional: real voice cloning backend (v2; not needed in simulated mode) ---
# See https://github.com/RVC-Boss/GPT-SoVITS
# torch==2.4.1
# torchaudio==2.4.1
